# Copy this file to `.env` and edit as needed
# Default connection points for local development
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=group_22

# Optional: configure test behaviour
# STRICT_MODE controls whether `tests/data_quality.mongosh.js` enforces exact
# seed counts from the fixtures. Set to `true` (default) or `false`.
STRICT_MODE=true
